"use strict";

/*
import httpStatus from 'http-status';
import request from 'supertest';
import chai from 'chai';
import { expect } from 'chai';
import { app } from '../../minion-config-server';

chai.config.includeStack = true;

describe('MinionConfig Server APIs', function () {
	describe('GET /healthcheck', function () {
		it('should get minion health status', function (done) {
			request(app)
				.get('/healthcheck')
				.send()
				.end(function (err, res) {
					expect(err).to.not.exist; // eslint-disable-line no-unused-expressions
					expect(res.statusCode).to.equal(httpStatus.OK);
					expect(res.body).to.equal(true);
					done();
				});
		});
	});
});
*/
//# sourceMappingURL=minion-config-service-unit-test.js.map
